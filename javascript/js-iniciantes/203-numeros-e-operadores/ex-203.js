// Qual o resultado da seguinte expressão?
var total = 10 + (5 * 2) / 2 + 20; // é igual a 35

// Crie duas expressões que retornem NaN
var expressao1 = 'Teste' / 2;
var expressao2 = 'Teste' - 2;
// Somar a string '200' com o número 50 e retornar 250
var soma = +'200' + 50;
// Incremente o número 5 e retorne o seu valor incrementado
var incremento = 5;
var valorIncrementado = ++incremento;
// Como dividir o peso por 2?
var numero = +'80'; // Adicionado o sinal de + antes do número, e convertendo ele para número
var unidade = 'kg';
var pesoPorDois = numero / 2 + unidade; 

console.log(pesoPorDois)
